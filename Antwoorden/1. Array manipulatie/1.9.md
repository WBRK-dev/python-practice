# Opdracht 1.9

## Het goede antwoord is: A

## Uitleg

`enumerate(arr)` geeft (index, waarde) paren: (0,"a"), (1,"b"), (2,"c"), (3,"d").  
`if i % 2 == 1` selecteert alleen **oneven** indexen: 1 en 3.  
`result += "b"` en `result += "d"` geeft `"bd"` (antwoord A).

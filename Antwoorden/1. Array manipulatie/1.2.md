# Opdracht 1.2

## Het goede antwoord is: A

## Uitleg

De loop itereert over `range(len(arr))`, dus `i` wordt 0 t/m 7.  
`if i % 2 == 0` selecteert alleen de **even indexen**: 0, 2, 4, 6.  
De bijbehorende waarden zijn `arr[0]=3`, `arr[2]=4`, `arr[4]=5`, `arr[6]=2`.  
Dus `result = [3, 4, 5, 2]` (antwoord A).

# Opdracht 2.7

## Het goede antwoord is: A

## Uitleg

`@classmethod` maakt een methode die aan de **klasse** is gebonden, niet aan een instantie.  
`Child.who()` roept de overschreven `who()` van `Child` aan, die `"Child"` teruggeeft.  
Resultaat: `"Child"` (antwoord A).

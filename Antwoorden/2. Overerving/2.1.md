# Opdracht 2.1

## Het goede antwoord is: B

## Uitleg

`print(racer.get_name())` roept `Person.get_name()` aan, die `self.__name` (gemanpled naar `_Person__name`) teruggeeft: `"Max Verstappen"`.  
`racer.print_certificate()` roept `Racer.print_certificate()` aan, die `self.__certificate` (gemanpled naar `_Racer__certificate`) print: `"FIA Super Licence"`.

Output:
```
Max Verstappen
FIA Super Licence
```

Antwoord B is correct.
